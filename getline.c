#include "main.h"

/**
 * _getline - implements the getline function in c
 *
 * Return: nothing
 */

/*ssize_t _getline(char **buff, size_t *n, FILE *stream)
{


	if (buff == NULL || n == NULL || stream == NULL)
	{
		return -1;
	}
*/
